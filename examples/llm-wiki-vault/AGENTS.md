# Agent Instructions

## Context Map

- Put raw interview notes, transcripts, and papers in `raw/`.
- Put durable concepts and project pages in `wiki/`.
- Put generated plans and deliverables in `outputs/`.

## Workflow

1. Read relevant raw sources before editing wiki pages.
2. Link wiki pages back to their sources using frontmatter `sources`.
3. Link outputs back to the wiki pages or raw sources they derive from.

## Stale Reference

The old folder `archive/raw-video/` no longer exists.
