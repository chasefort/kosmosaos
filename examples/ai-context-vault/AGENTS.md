# Agent Instructions

Use `raw/`, `wiki/`, and `outputs/` as the durable context system.

When you create an output, link it back to the source docs or wiki pages it depends on.

TODO: replace this starter policy with project-specific review rules.

Review `wiki/missing-playbook.md` before final delivery.
